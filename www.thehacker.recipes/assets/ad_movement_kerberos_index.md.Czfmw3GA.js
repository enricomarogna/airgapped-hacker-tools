import{_ as t}from"./chunks/Pass the things (dark).CprrsTzs.js";import{_ as a,c as s,a5 as o,o as i}from"./chunks/framework.B5CpDqM0.js";const f=JSON.parse('{"title":"Kerberos","description":"","frontmatter":{"authors":"ShutdownRepo, sckdev"},"headers":[],"relativePath":"ad/movement/kerberos/index.md","filePath":"ad/movement/kerberos/index.md","lastUpdated":1724982529000}'),r={name:"ad/movement/kerberos/index.md"};function n(c,e,d,l,h,u){return i(),s("div",null,e[0]||(e[0]=[o('<h1 id="kerberos" tabindex="-1">Kerberos <a class="header-anchor" href="#kerberos" aria-label="Permalink to &quot;Kerberos&quot;">​</a></h1><p>A Kerberos realm is a logical group of networked computers that share a common authentication database. The authentication database is used to store the Kerberos tickets that are issued to users and services when they authenticate to the network.</p><p>In a Kerberos environment, each networked computer is a member of a realm. The realm is responsible for authenticating users and services and for issuing and managing Kerberos tickets.</p><p>A Kerberos realm can be implemented on any type of network, including networks that are not running Windows. In fact, the Kerberos protocol was developed as an open standard and is used by many different types of operating systems and networks.</p><p>On a Windows network, a Kerberos realm is typically equivalent to a domain. Each domain in a Windows network is a member of a realm, and the domain controller is responsible for authenticating users and services and for issuing and managing Kerberos tickets.</p><p>However, on a non-Windows network, a Kerberos realm can be implemented independently of any domain structure. In this case, the Kerberos server is responsible for authenticating users and services and for issuing and managing Kerberos tickets.</p><p>The Hacker Recipes mainly covers Kerberos attacks in a Windows context, but some attacks can also be conducted in non-Windows networks.</p><p><img src="'+t+'" alt=""></p><h2 id="tickets" tabindex="-1">Tickets <a class="header-anchor" href="#tickets" aria-label="Permalink to &quot;Tickets&quot;">​</a></h2><p>Kerberos is an authentication protocol based on tickets. It basically works like this (simplified process):</p><ol><li>Client asks the KDC (Key Distribution Center, usually is a domain controller) for a TGT (Ticket Granting Ticket). One of the requesting user&#39;s keys is used for pre-authentication. The TGT is provided by the Authentication Service (AS). The client request is called <code>AS-REQ</code>, the answer is called <code>AS-REP</code>.</li><li>Client uses the TGT to ask the KDC for a ST (Service Ticket). That ticket is provided by the Ticket Granting Service (TGS). The client request is called <code>TGS-REQ</code>, the answer is called <code>TGS-REP</code>.</li><li>Client uses the ST (Service Ticket) to access a service. The client request to the service is called <code>AP-REQ</code>, the service answer is called <code>AP-REP</code>.</li><li>Both tickets (TGT and ST) usually contain an encrypted PAC (Privilege Authentication Certificate), a set of information that the target service will read to decide if the authentication user can access the service or not (user ID, group memberships and so on).</li></ol><p>A Service Ticket (ST) allows access to a specific service.</p><div class="tip custom-block"><p class="custom-block-title">cname formats</p><p>When requesting a service ticket, the client (<code>cname</code>) specifies the service it wants to obtain access to by supplying it&#39;s <code>sname</code>, which can be one of 9 types (<a href="https://www.rfc-editor.org/rfc/rfc4120#section-6.2" target="_blank" rel="noreferrer">RPC 4120 section 6.2</a>). Shortly put, the following formats are supported:</p><ul><li>servicePrincipalName</li><li>userPrincipalName</li><li>sAMAccountName</li><li>sAMAccountName@DomainNetBIOSName</li><li>sAMAccountName@DomainFQDN</li><li>DomainNetBIOSName\\sAMAccountName</li><li>DomainFQDN\\sAMAccountName</li></ul><blockquote><p>Note that if you use the <em>SRV01</em> string as a sAMAccountName, and the <em>SRV01</em> account does not exist, and the <em>SRV01$</em> account exists, this name will be treated as a principal name of the <em>SRV01$</em> account.</p><p><em>(</em><a href="https://swarm.ptsecurity.com/kerberoasting-without-spns" target="_blank" rel="noreferrer"><em>swarm.ptsecurity.com</em></a><em>)</em></p></blockquote><p>On a good note, if the service name is specified by something else than an SPN (i.e. SAN, UPN), Kerberos will basically deliver service tickets if the requested service</p><ul><li>has a trailing <code>$</code> in the requested SAN (<code>sAMAccountName</code>)</li><li>or has at least one SPN (<code>servicePrincipalName</code>)</li></ul><p>If the service ticket is requested through a specific <a href="./#user-to-user-authentication">U2U (User-to-User)</a> request, then neither of the conditions above will be required, the target <s>service</s> user can be specified by its UPN (<code>userPrincipalName</code>).</p><p><em>(sources:</em> <a href="https://twitter.com/SteveSyfuhs/status/1613956603807690753" target="_blank" rel="noreferrer"><em>Twitter</em></a> <em>and</em> <a href="https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-kile/a7ad31b0-37a4-4344-b9a7-01d4d086097e" target="_blank" rel="noreferrer"><em>[MS-KILE] section 3.3.5.1.1</em></a><em>).</em></p></div><p>The TGT is used to ask for STs. TGTs can be obtained when supplying a valid secret key. That key can be one of the following (read <a href="https://www.sstic.org/media/SSTIC2014/SSTIC-actes/secrets_dauthentification_pisode_ii__kerberos_cont/SSTIC2014-Article-secrets_dauthentification_pisode_ii__kerberos_contre-attaque-bordes_2.pdf" target="_blank" rel="noreferrer">more</a>).</p><table tabindex="0"><thead><tr><th>Key name (a.k.a. etype)</th><th>Details on key calculation</th></tr></thead><tbody><tr><td>DES</td><td>Key derivated from user&#39;s password</td></tr><tr><td>RC4</td><td>Key == NT hash</td></tr><tr><td>AES128</td><td>Key derivated from user&#39;s password (with salt)</td></tr><tr><td>AES256</td><td>Key derivated from user&#39;s password (with salt)</td></tr></tbody></table><div class="tip custom-block"><p class="custom-block-title">Salt used for key derivation</p><p></p><ul><li><strong>Users</strong>: uppercase FQDN + case sensitive username = <code>DOMAIN.LOCALuser</code></li><li><strong>Computers</strong>: uppercase FQDN + <code>host</code> + lowercase FQDN hostname without the trailing <code>$</code> = <code>DOMAIN.LOCALhostcomputer.domain.local</code></li></ul><p><em>(</em><a href="https://snovvcrash.rocks/2021/05/21/calculating-kerberos-keys.html" target="_blank" rel="noreferrer"><em>Kerberos keys calculation</em></a><em>)</em></p></div><p>Again, Microsoft has poorly implemented the zero-knowledge proof concept in Kerberos. An attacker knowing a user&#39;s NT hash could use it to ask the KDC for a TGT (if RC4 key is accepted). This is called <a href="./ptk">Overpass-the-hash</a>.</p><div class="tip custom-block"><p>Read the <a href="./ptk">Pass the key</a> article for more insight</p></div><p>Users are not the only ones whose NT hashes can be used to abuse Kerberos.</p><ul><li>A TGT is encrypted with the <code>krbtgt</code>&#39;s NT hash. An attacker knowing the <code>krbtgt</code>&#39;s NT hash can forge TGTs impersonating a domain admin. He can then request STs as a domain admin for any service. The attacker would have access to everything. This forged TGT is called a <a href="./forged-tickets/golden">Golden ticket</a>.</li><li>A ST is encrypted with the service account&#39;s NT hash. An attacker knowing a service account&#39;s NT hash can use it to forge a Service ticket and obtain access to that service. This forged Service ticket is called a <a href="./forged-tickets/silver">Silver ticket</a>.</li></ul><div class="tip custom-block"><p>Read the <a href="./forged-tickets/">Forged tickets</a> article for more insight</p></div><p><a href="./ptk">Overpass-the-hash</a>, <a href="./forged-tickets/silver">silver ticket</a> and <a href="./forged-tickets/golden">golden ticket</a> attacks are used by attackers to obtain illegitimate tickets that can then be used to access services using Kerberos without knowing any password. This is called <a href="./ptt">Pass-the-ticket</a>.</p><h2 id="roasting" tabindex="-1">Roasting <a class="header-anchor" href="#roasting" aria-label="Permalink to &quot;Roasting&quot;">​</a></h2><p>If Kerberos preauthentication is disabled for a user, it is possible to request a TGT for that specific user without knowing any credentials. When the TGT is requested, the KDC sends it along with a session key in the <code>KRB_AS_REP</code> message to the requesting client. The session key being encrypted with the requested user&#39;s NT hash, it is possible to crack that session key offline in a an attempt to find the user&#39;s password. This is called ASREProasting.</p><p>If an attacker finds himself in a man-in-the-middle position, effectively capturing Kerberos messages, he could capture <code>KRB_AS_REQ</code> messages and operate a similar cracking attempt.</p><div class="tip custom-block"><p>Read the <a href="./asreqroast">Asreqroast</a> article for more insight</p></div><p>When attackers have a foothold in the domain (i.e. valid domain credentials), they have the (intended) ability to request a service ticket (ST) for any valid SPN (ServicePrincipalName), or SAN (samAccountName). The ST being encrypted with the service account&#39;s NT hash, when that service account&#39;s password is weak, it is then possible to crack the ST offline in an attempt to find the password. This is called Kerberoasting. On a side note, obtaining a service ticket for a service specified by its SAN in an attempt to Kerberoast the account will only work if the service has at least one SPN.</p><p>As it turns out, AS-REQ messages can not only be used to request TGTs but can be invoked to ask for Service Tickets as well. One of the consequences of this is that Kerberoast can be conducted without prior foothold to the domain if the attacker knows the service to target (its SPN or name) as well as an ASREProastable username: <a href="./kerberoast#kerberoast-wo-pre-authentication">Kerberoasting without pre-authentication</a>.</p><h2 id="delegations" tabindex="-1">Delegations <a class="header-anchor" href="#delegations" aria-label="Permalink to &quot;Delegations&quot;">​</a></h2><p>Kerberos delegations allow services to access other services on behalf of domain users. For instance, this allows services to require access to other services&#39; data on the authenticated user&#39;s behalf in order to pull data that only the said user is supposed to have access to.</p><p>In some situations, Kerberos delegations can be abused by attackers to operate lateral movement or privilege escalation.</p><div class="tip custom-block"><p>Read the <a href="./delegations/">Delegations</a> article for more insight</p></div><p>In <a href="./delegations/#theory">some cases</a>, the delegation will not work. Depending on the context, the <a href="./delegations/bronze-bit">bronze bit </a>vulnerability (CVE-2020-17049) can be used to try to bypass restrictions.</p><div class="tip custom-block"><p>Read the <a href="./delegations/bronze-bit">Bronze bit</a> article for more insight</p></div><h2 id="service-for-user-extensions" tabindex="-1">Service-for-User extensions <a class="header-anchor" href="#service-for-user-extensions" aria-label="Permalink to &quot;Service-for-User extensions&quot;">​</a></h2><p>Kerberos delegations can be abused by attackers to obtain access to valuable assets and sometimes even escalate to domain admin privileges. Regarding <a href="./delegations/constrained">constrained delegations</a> and <a href="./delegations/rbcd">rbcd</a>, those types of delegation rely on Kerberos extensions called S4U2Self and S4U2Proxy.</p><ul><li>Service for User to Self (S4U2self): allows a service to obtain a Service Ticket, on behalf of another user (called &quot;principal&quot;), to itself.</li></ul><details class="details custom-block"><summary>S4U2self requirements</summary><p>There are no particular requirements to this extension. The service does not necessarily have to have an SPN, as long as the request&#39;s <code>sname</code> field is formatted correctly (see <a href="./#tickets">Tickets</a>). This requirement applies when <a href="./#s4u2self-+-u2u">combining S4U2self with U2U</a>.</p><p>The resulting Service Ticket is <code>forwardable</code> (i.e. can be used with S4U2Proxy to access another service) if, and only if:</p><ul><li>the service is configured for constrained delegation (KCD) with protocol transition</li><li>the principal is not &quot;sensitive for delegation&quot;</li><li>the principal is not a member of the Protected Users group</li></ul></details><ul><li>Service for User to Proxy (S4U2proxy): allows a service to obtain a Service Ticket, on behalf of a user to a different service.</li></ul><details class="details custom-block"><summary>S4U2proxy requirements</summary><p>For this extension to work properly, the service needs to supply a Service Ticket as &quot;additional-ticket&quot; (i.e. used as an evidence that the service using S4U2proxy has the authority to do it on behalf of a user).</p><p>For an S4U2proxy request to work and have the KDC issue a Service Ticket:</p><ul><li>the ST used as &quot;additional-ticket&quot; must have the forwardable flag set.</li><li>alternatively, in the <code>TGS-REQ</code>, in the pre-authentication data, the <code>PA-PAC-OPTIONS</code> structure must contain a padata value with the resource-based constrained delegation bit set (<a href="https://docs.microsoft.com/en-us/openspecs/windows_protocols/ms-sfu/aeecfd82-a5e4-474c-92ab-8df9022cf955" target="_blank" rel="noreferrer">doc</a>).<br><em>nota bene 1: this only applies if the resource-based constrained delegation (RBCD) is actually possible and authorized in the proper AD objects attributes.</em><br><em>nota bene 2: Rubeus and Impacket&#39;s getST always set that bit when doing S4U2proxy.</em></li></ul><p>On a side note, S4U2Proxy always results in a forwardable ST, even when the ticket used as evidence wasn&#39;t forwardable.</p></details><details class="details custom-block"><summary>More technical notes</summary><p>S4U2self and S4U2proxy are variations of Service Ticket requests (<code>KRB_TGS_REQ</code>). Below is what differentiates a S4U2self from a S4U2proxy from a standard <code>KRB_TGS_REQ</code>.​</p><ul><li>S4U2self</li><li>Request contains a <code>PA-FOR-USER</code> padata type structure containing the name and the realm of the user to impersonate (<a href="https://docs.microsoft.com/en-us/openspecs/windows_protocols/ms-sfu/aceb70de-40f0-4409-87fa-df00ca145f5a" target="_blank" rel="noreferrer">doc</a>).</li><li>the <code>cname</code> (user name authenticating) and the <code>sname</code> (service name being requested) are the same. In order to succeed and not have the KDC throw an <code>KDC_ERR_S_PRINCIPAL_UNKNOWN</code>, the <code>sname</code> should refer to an account that has at least one SPN (<code>Service Principal Name</code>) set.</li><li>S4U2proxy</li><li>Request contains an <code>additional-tickets</code> field containing a service ticket. In order to succeed and not have the KDC throw an <code>KDC_ERR_BADOPTION</code>, the ticket should have the <code>forwardable</code> flag set. In a standard constrained delegation or rbcd scenario, the ticket added in the <code>additional-tickets</code> field is the one obtained through S4U2self.</li><li>Request contains the <code>CNAME-IN-ADDL-TKT</code> flag in the <code>kdc-options</code> field, indicating S4U2proxy is used (<a href="https://docs.microsoft.com/en-us/openspecs/windows_protocols/ms-sfu/17b9af82-d45a-437d-a05c-79547fe969f5" target="_blank" rel="noreferrer">doc</a>).</li></ul></details><h2 id="user-to-user-authentication" tabindex="-1">User-to-User authentication <a class="header-anchor" href="#user-to-user-authentication" aria-label="Permalink to &quot;User-to-User authentication&quot;">​</a></h2><p>U2U has nothing to do with S4U mechanisms and plays no part in delegation internals.</p><blockquote><p>[U2U] allows users to host secure application services on their desktop machines. [...] In the user-to-user protocol, one user acts as a server, and the other user acts as a client. (<a href="http://www.di-srv.unisa.it/~ads/corso-security/www/CORSO-0001/kerberos/ref/kerberos-faq.html#u2uauth" target="_blank" rel="noreferrer">Frequently Asked Questions about Kerberos</a>).</p></blockquote><details class="details custom-block"><summary>More technical notes</summary><p>A U2U request is a variation of a common Service Ticket request (<code>KRB_TGS_REQ</code>). Below is what differentiates a U2U from a standard <code>KRB_TGS_REQ</code>. It allows a user to request a service ticket to another user.</p><ul><li>Request contains an <code>additional-tickets</code> field containing the target user TGT.</li><li>Request contains the <code>ENC-TKT-IN-SKEY</code> flag in the <code>kdc-options</code> field, indicating that the ticket for the end server is to be encrypted in the session key from the additional TGT provided (<a href="https://docs.microsoft.com/en-us/openspecs/windows_protocols/ms-sfu/17b9af82-d45a-437d-a05c-79547fe969f5" target="_blank" rel="noreferrer">doc</a>).</li><li>The <code>sname</code> refers to a UPN (<code>User Principal Name</code>) of an account that doesn&#39;t necessarily have to have an SPN set.</li></ul><p>A ticket produced with u2u is encrypted with the session key of the TGT included as <code>additional-ticket</code> instead of the target service long-term key (i.e. DES, RC4 or AES128/256 key).</p></details><details class="details custom-block"><summary>S4U2self + U2U</summary><p>In some specific scenarios, S4U2self and U2U can be combined, in which case the flags and structures both mechanisms include in their requests are combined.</p><p>This allows to</p><ul><li>operate <a href="./delegations/rbcd#rbcd-on-spn-less-users">RBCD attacks from SPN-less accounts</a></li><li>operate an <a href="./unpac-the-hash">unPAC-the-hash</a> attack</li><li>retrieve and decrypt the PAC (Privileged Attribute Certificate) of any account. Could be used to obtain a <a href="./forged-tickets/sapphire">sapphire ticket</a>.</li></ul></details>',46)]))}const b=a(r,[["render",n]]);export{f as __pageData,b as default};
